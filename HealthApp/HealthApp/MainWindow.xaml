<Window x:Class="HealthApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HealthApp"
        mc:Ignorable="d"
        Title="HealthApp" Height="450" Width="800">
    <Grid>
        <!-- Sidebar using UniformGrid -->
        <UniformGrid Columns="1"
                     Rows="6"
                     Background="#FF1E1E1E"
                     Width="200"
                     HorizontalAlignment="Left"
                     VerticalAlignment="Stretch">

            <UniformGrid.Resources>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="#FF2D2D30"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontSize" Value="16"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    <Setter Property="Padding" Value="20,0"/>
                    <Setter Property="Margin" Value="0,0"></Setter>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                    <ContentPresenter VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FF3E3E42"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#FF505053"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </UniformGrid.Resources>

            <!-- Buttons -->
            <Button Name="TrackerPaneButton" Content="🍂  Workout Tracker" Click="TrackerPaneButton_Click"/>
            <Button Name="DataViewPaneButton" Content="📅  Data View" Click="DataViewPaneButton_Click"/>
            <Button Name="FoodReferencePaneButton" Content="🍊  Food Reference" Click="FoodReferencePaneButton_Click" />
            <Button Name="TimerPaneButton" Content="⏲️  Timer" Click="TimerPaneButton_Click"/>
            <Button Name="SettingsPaneButton" Content="⚙️  Settings" Click="SettingsPaneButton_Click"/>
            <Button Name="ExitButton" Content="👋  Exit" Click="ExitButton_Click"/>
        </UniformGrid>

        <!-- Main Content -->
        <Grid Margin="200,0,0,0" x:Name="MainContent">
            <Grid.Background>
                <ImageBrush ImageSource="/Assets/default_bg.jpg"/>
            </Grid.Background>
            <ContentControl Content="{Binding CurrentPane}">
                
            </ContentControl>
        </Grid>
    </Grid>
</Window>
